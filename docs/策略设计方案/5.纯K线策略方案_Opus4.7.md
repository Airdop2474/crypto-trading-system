# 纯 K 线 BTC 策略方案设计文档（精简版）

> 作者: Opus 4.7
> 日期: 2026-06-26
> 数据基础: BTCUSDT 1m/5m/15m/30m/1h/4h K 线（2024-01 至 2026-06），含 OHLCV / trades / taker volume
> 目标: 不使用任何技术指标，仅基于 K 线形态/结构/价格行为开发策略
>
> 说明: 本文档是 7 轮纸面推敲（原 2782 行/36 Part）后的精简版，保留可落地的核心内容。
> 完整推敲历史归档于 `5.纯K线策略方案_Opus4.7_full.md`。
> 砍掉的 29 个 Part 主要是元层级反思（博弈论/认知偏误/过度工程检讨/推敲天花板诚实记录等），
> 对实施无直接价值。

---

## Part 0: 设计前的共识

### 0.1 什么算"纯 K 线"

明确范围避免后期争议：

- ✅ 可以用：OHLC、volume、trades count、taker volume、时间维度（小时、星期）、相对位置（高低点序列、ranges）
- ❌ 不用：MA、EMA、RSI、MACD、Bollinger、ADX、ATR 等公式化指标
- ⚠️ 灰色地带：ATR 的"思想"可以用（如"过去 N 根 range 中位数"），但不命名为 ATR

### 0.2 评估维度

不能只看胜率。每个策略要回答：

1. 信号触发频率（一年多少次）→ 影响样本量
2. 胜率 × R > 1 + 手续费 + 滑点
3. 最大回撤、回撤持续时间
4. 不同市场环境（趋势/震荡/暴跌）的表现差异
5. 参数稳健性（±20% 变化后还能用吗）

### 0.3 BTC 特性

- 7×24 无开盘缺口，但有 **session liquidity 差异**（Asia/London/NY）
- 整数关口（50k、60k、70k、100k）是真实流动性磁石
- 周末成交量降 40-60%，噪声高
- 现货 vs 永续：永续有资金费率，但 K 线形态接近
- 滑点估算：1h/4h 策略 0.05%，5m/15m 要 0.1%

### 0.4 共同陷阱

1. **Look-ahead bias**：swing 定义需要"未来 N 根"，回测时只能在第 N+1 根使用
2. **重复计数**：同一波行情多次进场（必须有冷却期或 in_position 状态）
3. **Survivorship**：2024-2026 是 BTC 牛市后期 + 巨幅波动期，不代表通用环境
4. **参数过拟合**：训练样本要严格留出 2026 样本外

---

## Part 1: 时间周期选择

| 策略类型 | 推荐周期 | 理由 |
|---|---|---|
| 市场结构 | 4h 主、1h 辅 | swing 点"重"，BOS/CHoCH 信号干净 |
| 流动性扫荡 | 1h | 平衡频率与信号质量 |
| FVG | 15m / 1h | 高周期 FVG 少；低周期噪声大 |
| 波动压缩 | 1h / 4h | 压缩需要时间累积 |
| 影线拒绝 | 4h | 长影线在高周期才有统计意义 |

**主推：1h**（数据 8761 根足够回测）；**辅助：4h**（趋势过滤）。

---

## Part 2: 核心五方案（A-E）

每个方案统一结构：核心假设 → 精确定义 → 进场规则 → 出场规则 → 过滤器 → 参数 → 失败模式 → 与其他方案关系。

---

### 方案 A：市场结构策略（Structure-based Trend Following）

#### A.1 核心假设

价格走势 = 高低点序列演变。CHoCH 出现后新趋势平均持续 N 根 K 线以上，提供顺势机会。

#### A.2 精确定义

**Swing High（左右各 N 根）**
- 给定 K[i]，若 K[i].high 严格大于 K[i-N..i-1] 和 K[i+1..i+N] 的所有 high → swing high
- N=3（1h 周期默认）
- 在 K[i+N] 收盘后才"确认"，避免 look-ahead

**Swing Low** 对称定义。

**结构状态机**
- `Bull`：最近一次 swing 形成 higher high 或 higher low
- `Bear`：最近一次 swing 形成 lower high 或 lower low
- `Neutral`：初始 / 刚 CHoCH 未形成新结构

**BOS / CHoCH**
- BOS：Bull 状态下，收盘 > 最近未被突破的 swing high → 趋势延续
- CHoCH：Bull 状态下，收盘 < 最近 swing low → 潜在反转 → 状态→Neutral

#### A.3 进场规则（做多，空对称）

1. 状态从 Bear/Neutral 切到 Bull（首次 BOS up 或 CHoCH up）
2. 等待回调：价格回到最近的 swing low 上方 0.3% 以内
3. 出现 bullish confirmation：当前 K 线 close > 前 K 线 high
4. 进场：confirmation K 线收盘价
5. 冷却：进场后 24 根（1h 周期 = 24 小时）内不再进场

#### A.4 出场规则

- **止损**：依赖的 swing low 下方 0.1%
- **初始止盈**：1:2 R
- **Trailing**：每形成新的 higher low → 止损上移到该 low 下方，不下调
- **时间止损**：72 根（3 天）未触发任何条件，市价平仓
- **结构失效**：CHoCH down 出现，立即市价平仓

#### A.5 过滤器（叠加测试）

- **4h 趋势过滤**：仅在 4h 也是 Bull 时做多
- **波动过滤**：过去 20 根 range 标准差 < 中位数 0.5×（极静）不进场
- **Session 过滤**：可选只在 London/NY session（北京时间 16:00-04:00）入场

#### A.6 参数

| 参数 | 默认 | 测试网格 |
|---|---|---|
| swing N | 3 | [2, 3, 5, 8] |
| 回调容差 | 0.3% | [0.2%, 0.3%, 0.5%, 1%] |
| RR 目标 | 2 | [1.5, 2, 3] |
| 冷却期 | 24 bar | [12, 24, 48] |
| 时间止损 | 72 bar | [48, 72, 120] |

#### A.7 已知失败模式

1. **横盘震荡**：swing 来回切换，频繁进出 → 需波动过滤
2. **闪崩单根穿透**：止损被瞬间扫又回来 → 用收盘价判定止损
3. **慢牛 grinding up**：没清晰回调永远进不去 → 接受"漏单"，不降低标准

#### A.8 与其他方案的关系

- 与 B 互补：A 给方向，B 给精确入场
- 与 E（影线）正交：方向相反
- 与 D（压缩）互补：A 趋势中段，D 趋势启动

---

### 方案 B：流动性扫荡（Liquidity Sweep / Stop Hunt Reversal）

#### B.1 核心假设

参与者集中把止损放在近期等高/等低点之上。机构需要这些流动性建仓，扫掉后常反向运行。

#### B.2 精确定义

**Equal Highs（等高点）**
- 过去 L=50 根内找所有 swing high
- 2 个以上 swing high 价格差异 < 0.1%（BTC 约 50-100 美元）→ 等高点群
- 取最高值作为"流动性位"

**Sweep 事件**
- 当前 K 线 high > 流动性位
- 当前 K 线 close < 流动性位（必须收回去）
- 该流动性位首次被触及

#### B.3 进场规则（做空，做多对称）

1. 检测到等高点 sweep
2. **确认 K 线**：sweep K 线的下一根 close < sweep K 线 close
3. 进场：确认 K 线收盘做空
4. 已识别等高点群标记为"已扫"，48 根内不再视为有效

#### B.4 出场规则

- **止损**：sweep K 线 high 之上 0.1%（很紧，论点就是"价格不应再上去"）
- **初始止盈**：下方第一个未被扫的等低点 / RR=2 取近
- **部分止盈**：50% 在 RR=1，剩余 trailing
- **Trailing**：每形成新 lower high → 止损下移到该 high 上方
- **时间止损**：24 根（1 天）
- **反向 sweep**：立即平仓

#### B.5 过滤器

- **4h 趋势**：仅在 4h 不是强 bull 时做空（避免强趋势顶部抄底空）
- **Volume 确认**：sweep K 线 volume > 过去 20 根均值 1.3×
- **Wick 比例**：sweep K 线上影线 / 总 range > 0.4
- **整数关口加成**：流动性位接近 10k/5k 整数，置信度+

#### B.6 参数

| 参数 | 默认 | 测试范围 |
|---|---|---|
| Lookback L | 50 | [30, 50, 100] |
| 等高容差 | 0.1% | [0.05%, 0.1%, 0.2%] |
| R 目标 | 2 | [1.5, 2, 3] |
| 不重复触发期 | 48 bar | [24, 48, 96] |
| 时间止损 | 24 bar | [12, 24, 48] |

#### B.7 已知失败模式

1. **真突破**：扫荡后继续突破 → 紧止损打掉，但损失有限
2. **趋势衰竭误判**：单边趋势中所有 swing high 都被扫但每次都继续 → 4h 过滤
3. **流动性位定义过严**：找不到等高点 → 无信号；过松 → 信号太多
4. **K 线粒度问题**：5m 扫损后，1h 可能合并显示为长上影线 close < high

#### B.8 与其他方案关系

- 与 A 互补：A 趋势方向，B 反向流动性回归入场
- 与 C 互补：sweep 后常形成 FVG（暴力回收造成跳空区域），可串联
- 与 E 接近但更强：E 仅看单根 wick，B 要求 wick + 上方有明确流动性

---

### 方案 C：FVG 回填策略（Fair Value Gap Mean Reversion）

#### C.1 核心假设

快速移动留下"未充分定价"的区间。后续行情倾向于回填，作为支撑/阻力。

#### C.2 精确定义

**Bullish FVG**（三根 K 线 bar[i-2], bar[i-1], bar[i]）
- bar[i].low > bar[i-2].high
- 区间 = [bar[i-2].high, bar[i].low]
- 强化条件：bar[i-1] 实体 > bar[i-1].range × 0.6（实体主导）

**FVG 状态**
- 未填补 / 部分填补 / 已填补 (mitigation) / 失效（创建后 50 根未触及）

#### C.3 进场规则

**FVG 作为支撑做多**：
1. 存在未填补 bullish FVG
2. 当前 K 线 low 进入 FVG 区间但未穿透下沿
3. 当前 K 线 close > FVG 中点
4. 进场：当前 K 线收盘做多

#### C.4 出场规则

- **止损**：FVG 下沿之下 0.1%
- **止盈 T1**：FVG 高度的 1.5 倍
- **止盈 T2**：下一个未填补反向 FVG / 前 swing high
- **FVG 失效**：价格收盘 < FVG 下沿，立即平仓
- **时间止损**：48 根

#### C.5 过滤器

- **结构方向**：仅做多 bullish FVG 且 1h 结构为 Bull（与 A 联动）
- **FVG 大小**：< 0.3% 太小（噪声），> 3% 太大（创建时已暴跌）
- **创建后等待**：FVG 创建后至少 3 根才考虑入场（避免追单）
- **同向 FVG 群**：3-5 根内有 2+ 个同向 FVG → 信号更强

#### C.6 参数

| 参数 | 默认 | 测试范围 |
|---|---|---|
| 实体占比阈值 | 0.6 | [0.5, 0.6, 0.7] |
| FVG 最小高度 | 0.3% | [0.2%, 0.3%, 0.5%] |
| FVG 最大高度 | 3% | [2%, 3%, 5%] |
| 失效根数 | 50 | [30, 50, 100] |
| 创建后等待 | 3 bar | [0, 3, 5] |

#### C.7 已知失败模式

1. **暴跌中 FVG 太多**：自由落体每根都创建 FVG → 必须过滤"已经在远端"的
2. **FVG 嵌套**：FVG 内还有 FVG，难判定哪个是真支撑
3. **远端 FVG 永不回填**：BTC 突破 100k 后下方所有 FVG 都是"理论支撑"，永远不回 30k → 失效根数过滤
4. **多空 FVG 同时存在打架**：信号矛盾

#### C.8 与其他方案关系

- 强烈推荐与 A 联动：A 定方向，C 找回踩入场区
- 与 B 互补：sweep 后常形成 FVG，二者可串联

---

### 方案 D：波动压缩-扩张（Volatility Compression Breakout）

#### D.1 核心假设

波动率均值回归：低波动后必有高波动，扩张方向往往是趋势启动方向。

#### D.2 精确定义

**Range（单根）**：high - low

**压缩状态**
- 过去 W=5 根，每根 range 都 < 过去 R=20 根 range 中位数 × 0.7
- 5 根整体 high - low < 过去 20 根平均 range 的 2×

**压缩区间**：连续压缩根的 max(high) 和 min(low)

#### D.3 进场规则

**做多**：
1. 检测到压缩状态
2. 当前 K 线 close > 压缩区间上沿
3. 当前 K 线 close > open（向上实体）
4. 进场：当前 K 线收盘

#### D.4 出场规则

- **止损**：压缩区间另一端
- **初始止盈**：压缩区间高度的 2 倍
- **Trailing**：用 swing low 移动止损
- **反向突破**：收盘 < 压缩区间下沿立即平仓
- **时间止损**：36 根

#### D.5 过滤器

- **实体过滤**：突破 K 线实体 > 整根的 0.5（避免长影线假突破）
- **Volume 加成**：突破 K 线 volume > 前 5 根均值 1.5×
- **4h 方向**：仅顺 4h 趋势做突破方向
- **整体波动环境**：过去 100 根平均 range > 4%（极度高波）时压缩信号不可靠

#### D.6 参数

| 参数 | 默认 | 测试范围 |
|---|---|---|
| 压缩窗口 W | 5 | [3, 5, 8, 10] |
| 中位数回看 R | 20 | [15, 20, 30] |
| 压缩阈值 | 0.7 | [0.5, 0.7, 0.85] |
| RR 目标 | 2 | [1.5, 2, 3] |

#### D.7 已知失败模式

1. **假突破**：突破立刻回归 → close 而非 wick + volume 确认
2. **趋势中无压缩**：强趋势从不真正压缩 → 该策略缺信号
3. **压缩内部回旋**：W=5 太短，可能是趋势中小停顿
4. **周末压缩误判**：周末压缩是流动性低导致，不是趋势酝酿

#### D.8 与其他方案关系

- 与 A 互补：A 趋势中段，D 趋势启动
- 不要与 B 同时用：信号方向可能相反

---

### 方案 E：影线拒绝（Wick Rejection at Extreme）

#### E.1 核心假设

单根 K 线在极值位置出现长影线 = 该方向被强力拒绝，反向短线机会。

#### E.2 精确定义

**长影线 K 线**
- range = high - low；上影 = high - max(o,c)；下影 = min(o,c) - low；实体 = |c-o|
- 顶部拒绝：上影 / range > 0.6 且 实体 / range < 0.3
- 底部拒绝对称

**极值位置**
- K 线 high 为过去 N=20 根新高（顶部场景）

#### E.3 进场规则（顶部拒绝做空）

1. 检测到顶部拒绝长影线
2. **不立即进场**，等下一根 close < 拒绝 K 线 close 确认
3. 进场：确认 K 线收盘做空

#### E.4 出场规则

- **止损**：拒绝 K 线 high 之上 0.1%
- **初始止盈**：最近 swing low 或 RR=1.5
- **时间止损**：12 根

#### E.5 过滤器

- **Volume 确认**：拒绝 K 线 volume > 过去 20 根均值 1.5×
- **趋势过滤**：仅在 4h 反向时做反转，或顺势作为回踩入场触发器
- **整数关口加成**：影线最高点接近整数关口
- **时段过滤**：仅在主流动性时段（London/NY 开盘）有效

#### E.6 参数

| 参数 | 默认 | 测试范围 |
|---|---|---|
| 影线比例 | 0.6 | [0.5, 0.6, 0.7] |
| 实体比例上限 | 0.3 | [0.2, 0.3, 0.4] |
| 极值回看 N | 20 | [10, 20, 50] |
| RR 目标 | 1.5 | [1, 1.5, 2] |

#### E.7 已知失败模式

1. **趋势内的小拒绝**：上涨中每个 4h 都有上影但每次继续上 → 必须配合更高层级方向
2. **过小 K 线**：range 本身很小，影线占比高但波动无意义 → 过滤 range 最小值
3. **样本量大单次盈亏小**：信号频繁、单次价值低

#### E.8 与其他方案关系

- 与 B 接近但更弱：B 要求明确流动性位
- 可作为 A、C 的入场触发器
- 不要单独作为主策略

---

## Part 3: 补充方案（F/G/H）

补充 3 个维度：K 线序列叙事、单根吞没反转、订单流偏向。

---

### 方案 F：K 线序列叙事（Bar Sequence / Momentum Sequence）

#### F.1 核心假设
连续 N 根同向 K 线 + 实体逐渐放大 = 单边动能在累积，下一根继续同向概率高于 50%。这是"动量"的纯 K 线表达，不用 MA。

#### F.2 精确定义

**累积序列（做多）**：
- 连续 K=3 根 bull K 线（close > open）
- 每根 body_ratio > 0.5
- 后一根 body ≥ 前一根 body × 0.8
- 序列内最后一根 close = 序列内最高 close（无内部回吐）

**衰竭信号**：
- 累积序列出现后，下一根 body_ratio < 0.3（犹豫）→ 暂停
- 出现反向吞没 → 趋势终结

#### F.3 进场规则（顺势做多）

1. 检测到 3 根累积 bull 序列
2. 等待 1 根回调（close < open 但 close > 序列首根 open）
3. 进场：回调根的下一根 close > 前根 high 时入场
4. 冷却：12 根

#### F.4 出场规则
- **止损**：累积序列首根 low 下方 0.1%
- **止盈**：RR=1.5（动量类止盈要近，均值回归压力大）
- **衰竭止损**：反向吞没立即平仓
- **时间止损**：24 根

#### F.5 过滤器
- 配合方案 A：仅在结构为 Bull 时启用
- 排除暴跌后反弹：序列前 50 根有 -10% 跌幅则跳过
- taker_buy/total > 0.55 → 真实买盘主导

#### F.6 参数
| 参数 | 默认 | 范围 |
|---|---|---|
| 累积根数 K | 3 | [2, 3, 4, 5] |
| body_ratio 阈值 | 0.5 | [0.4, 0.5, 0.6] |
| body 递减容差 | 0.8 | [0.7, 0.8, 0.9] |
| RR | 1.5 | [1, 1.5, 2] |

#### F.7 失败模式
- 序列在阻力位前形成 → 高位接盘
- 周末低流动性下小 K 线连续触发 → range 最小值过滤

#### F.8 关系
- 与 D 互补：D 看波动，F 看方向序列
- 与 A 同向但更细粒度

---

### 方案 G：吞没反转（Engulfing at Range Extreme）

#### G.1 核心假设
吞没是更明确的反转信号，但必须在"区间极值"才有意义。

#### G.2 精确定义

**Bullish Engulfing**：
- bar[i].open ≤ bar[i-1].close
- bar[i].close > bar[i-1].open
- bar[i] 实体 > bar[i-1] 实体 × 1.5

**区间极值**：bar[i-1].low ≤ 过去 N=20 根最低 low

#### G.3 进场规则（做多）
1. 出现 bullish engulfing 且在区间低点
2. 等待 1 根确认：close > engulfing K 线 high
3. 进场：确认 K 线收盘

#### G.4 出场规则
- **止损**：engulfing 组合最低点下方 0.1%
- **止盈 T1**：50% 在 RR=1
- **止盈 T2**：剩余 50% trailing 至 swing low
- **时间止损**：18 根

#### G.5 过滤器
- volume：engulfing K 线 volume > 前 20 根均值 1.5×
- 下跌后吞没，不是上涨中吞没：检查过去 10 根最大跌幅 > 3%
- 与方案 B 冲突时优先 B

#### G.6 参数
| 参数 | 默认 | 范围 |
|---|---|---|
| 实体倍数 | 1.5 | [1.2, 1.5, 2.0] |
| 极值回看 N | 20 | [10, 20, 50] |
| RR T1 | 1 | [0.8, 1, 1.5] |

#### G.7 失败模式
- "假吞没"：实体大但下一根继续下跌 → 等确认 K 线
- 吞没后窄幅震荡 → 时间止损

#### G.8 关系
- 取代方案 E 成为独立策略（E 改为"形态库"中的辅助过滤）
- 与 B 互补：二者重叠时信号最强

---

### 方案 H：订单流偏向（Taker Volume Bias）

#### H.1 核心假设
taker_buy_volume / total_volume 接近 1 = 主动买盘极强；接近 0 = 主动卖盘极强。这是"市场情绪"的纯数据表达。

#### H.2 精确定义

**Taker 买盘强度**：tbr = taker_buy_base_volume / volume，范围 [0, 1]

**极端读数**：
- 极强买：tbr > 0.65
- 极强卖：tbr < 0.35

**累积偏向**：过去 5 根均值 tbr > 0.6 → 持续买盘

#### H.3 进场规则

**用作过滤器（推荐）**：所有其他策略的多头信号都要求 tbr > 0.5；空头要求 tbr < 0.5。

**用作独立信号（弱）**：
- 价格在过去 50 根低点附近（10% 范围）
- 出现 tbr > 0.7 的单根 K 线
- 该 K 线 close > open
- 进场：下一根 close 做多

#### H.4 出场规则（独立信号场景）
- 止损：信号 K 线 low 下方 0.1%
- 止盈：RR=1.2（低预期 + 高频）
- 时间止损：12 根

#### H.5 失败模式
- 现货 vs 永续 taker 行为不同 → 必须明确使用哪个交易所数据
- 单根 tbr 极值经常是噪声 → 用 5 根均值更稳
- 与价格背离：tbr 高但价格不涨 → 慎入

#### H.6 关系
- 作为 A-G 所有策略的过滤器最有价值
- 独立信号弱，不推荐单独成策略

---

## Part 4: 工程实施

### 4.1 计算量预估

- 8 个策略 × ~5 个核心参数 × ~4 个取值
- 网格组合：5^4 = 625（实际用 LHS 采样 ~50 组）
- Walk-forward 6 折
- 单次回测：8761 根 × ~5ms = 44s

预估：8 × 50 × 6 × 44s = **~6 小时**（单进程）。多进程 4-8 倍加速 → 1 小时内。可接受。

### 4.2 数据预处理（必须先做）

1. 加载 1h CSV → DataFrame
2. 完整性检查：相邻 K 线时间差 = 1h，缺失则报错
3. 异常检查：range / median > 5 的 K 线标记
4. 派生列：body, upper_wick, lower_wick, range, body_ratio, tbr, hour_of_day, day_of_week
5. 多周期对齐：1h 数据聚合出 4h
6. 缓存为 parquet

### 4.3 信号 → 交易的标准接口

每个策略输出标准格式：
```
signals = [
    {
        'bar_index': int,
        'timestamp': datetime,
        'direction': 'long' | 'short',
        'entry_price': float,
        'stop_loss': float,
        'take_profit_1': float,
        'take_profit_2': float | None,
        'time_stop_bars': int,
        'confidence': float in [0, 1],
        'strategy': str,
        'metadata': dict,
    }
]
```

回测引擎只需理解这一层接口，与策略实现解耦。

### 4.4 与现有 strategy_evaluator 的集成路径

集成方式两选：
1. **新策略直接接入 strategy_evaluator**：实现现有接口，复用 MC 模拟
2. **独立模块先跑研究**：纯 K 线策略放在 `src/strategies/price_action/`，研究通过后再桥接

**推荐方案 2**：研究阶段不污染现有系统，验证有效后再集成。

---

## Part 5: 数学边界 — 最低胜率门槛

### 5.1 成本模型

币安永续合约典型参数：
- 双边手续费：0.04% × 2 = 0.08%
- 双边滑点：1h 周期取 0.05% × 2 = 0.10%
- 总成本：约 0.18%

| 周期 | 典型 SL | cost / SL |
|---|---|---|
| 1h | 0.8% - 1.5% | 12% - 22% |
| 4h | 1.5% - 3% | 6% - 12% |
| 15m | 0.3% - 0.6% | 30% - 60% |

**结论**：15m 周期成本占止损 30%+，胜率门槛过高，不适合作为主战场。

### 5.2 盈亏平衡胜率公式

```
E[trade] = WR × R × SL − (1 − WR) × SL − cost_dollars
令 E > 0：WR > (1 + cost_ratio) / (R + 1)
```

### 5.3 各 RR 下的最低胜率（1h cost_ratio = 18%）

| RR | 数学下限 | 加 10% 安全边际 | 实际目标 |
|---|---|---|---|
| 1.0 | 59% | 65% | 65%+ |
| 1.5 | 47% | 52% | 52%+ |
| 2.0 | 39% | 43% | 43%+ |
| 2.5 | 34% | 38% | 38%+ |
| 3.0 | 30% | 33% | 33%+ |

### 5.4 各方案的胜率门槛筛选

| 方案 | 默认 RR | 期望 WR | 门槛 WR | 通过 |
|---|---|---|---|---|
| A 结构 | 2.0 | 45-55% | 43% | ✅ |
| B Sweep | 2.0 | 50-60% | 43% | ✅✅ |
| C FVG | 1.5 | 50-55% | 52% | ⚠️ 临界 |
| D 压缩 | 2.0 | 35-45% | 43% | ⚠️ 临界 |
| E 影线 | 1.5 | 45-55% | 52% | ⚠️ 临界 |
| F 序列 | 2.0 | 40-50% | 43% | ⚠️ |
| G 吞没 | 1.5 | 50-60% | 52% | ✅ |
| H Taker | — | 过滤器 | — | — |

**关键判断**：
- A、B、G 通过；C、D、E、F 临界
- 临界方案应优先在 4h 周期测试（cost_ratio 减半）
- 任何方案回测胜率离门槛 < 5% 都视为"无效"

### 5.5 部分止盈的成本

分批出场要算 **两次** 成本！如果 SL = 1%，第一次部分止盈实际净赚 = 1% × 50% − 0.18% × 50% = 0.41%。

---

## Part 6: 数据/特征预计算清单

一次性算好所有"原始特征"，后续策略只查表。

### 6.1 K 线级特征（每根一行）

| 字段 | 计算 |
|---|---|
| range | high − low |
| body | abs(close − open) |
| body_ratio | body / range |
| upper_wick | high − max(open, close) |
| lower_wick | min(open, close) − low |
| upper_wick_ratio | upper_wick / range |
| lower_wick_ratio | lower_wick / range |
| direction | sign(close − open) |
| taker_buy_ratio | taker_buy_base / volume |
| session | f(close_time_utc) |
| hour_of_day | 0-23 |
| day_of_week | 0-6 |

### 6.2 滚动窗口特征

| 字段 | 窗口 |
|---|---|
| range_median_20 | 20 |
| range_median_50 | 50 |
| volume_mean_20 | 20 |
| highest_20 | 20 |
| lowest_20 | 20 |
| consec_up | 滚动 |
| consec_down | 滚动 |

### 6.3 事件级特征（稀疏）

| 字段 | 说明 |
|---|---|
| swing_high_3 / swing_low_3 | N=3 swing |
| swing_high_5 / swing_low_5 | N=5 swing |
| bullish_fvg / bearish_fvg | FVG 创建根 |
| fvg_high / fvg_low | FVG 上下沿 |
| equal_highs_id | 等高点群 ID |
| sweep_event | 1 = 发生 sweep |
| engulf_bull / engulf_bear | 吞没形态 |

### 6.4 衍生状态（状态机维护）

- 当前 structure_state（Bull/Bear/Neutral）
- 活跃 FVG 池子
- 活跃流动性位池子

### 6.5 预计算流程

```
raw_csv → load_ohlcv → add_kbar_features → add_rolling_features
       → detect_swings → detect_fvgs → detect_sweeps → detect_patterns
       → save_features_parquet → save_events_parquet
```

执行一次约 30 秒，后续回测全部走 parquet。

---

## Part 7: 组合 I（A+B+C）协作规则

### 7.1 三策略的角色

| 策略 | 角色 |
|---|---|
| A 结构 | 方向过滤器 + 独立信号源 |
| B Sweep | 入场触发器 |
| C FVG | 入场触发器（被动等待） |

### 7.2 决策矩阵（每根 K 线收盘后执行）

```
1. 更新 A 的 structure_state
2. 检查 B 是否有 sweep_event
3. 检查当前价格是否触发任何活跃 FVG
4. 应用以下决策树：

if structure_state == Bull:
    优先级 1: A 自身回调入场（最严格）
    优先级 2: 多头 FVG 触发 + 满足 A 的方向
    优先级 3: 多头 sweep 触发 + 满足 A 的方向
    禁止: 任何空头信号

elif structure_state == Bear:
    （对称）

elif structure_state == Neutral:
    禁止: A 的入场信号
    允许（低仓位）: B sweep 信号
    禁止: C FVG
```

### 7.3 仓位分配

**单 BTC 账户最大并发仓位：1**

仓位大小：
- A 自身入场：1.0 R
- A 方向 + C FVG：0.8 R
- A 方向 + B sweep：1.2 R
- Neutral 状态下的 B sweep：0.5 R

R = 总资金 × 单笔风险（如 1%）

### 7.4 信号同时出现的处理

| A | B | C | 决策 |
|---|---|---|---|
| 多 | 多 | 多 | 取 A，1.2 R（强共振） |
| 多 | 多 | - | 取 A 或 B 先到的，1.0 R |
| 多 | - | 多 | 取 A，C 作为加仓点（暂不实施） |
| 多 | 空 | - | 取 A，忽略 B |
| 中性 | 多 | - | B 单独，0.5 R |
| 中性 | 空 | - | B 单独，0.5 R |
| 空 | 多 | - | 忽略 |
| - | 多 | 空 | 全部跳过 |

### 7.5 持仓中再出现信号

- **同向新信号**：默认忽略（避免加仓到反转点）
- **反向信号**：不平仓、不反向，仅记录
- **structure_state 翻转**：立即按 A 退出规则平仓，等 6 根再考虑反向

### 7.6 退出规则不混用

不同进场策略用各自的退出规则：
- A 入场 → 用 A 的 swing-based trailing
- B 入场 → 用 B 的反向 sweep / 时间止损
- C 入场 → 用 FVG 失效 + RR 目标

### 7.7 监控指标

每周输出：
- 每个子策略的信号数（A、B、C 各贡献多少）
- 每个子策略的胜率与 R 倍数
- 信号冲突次数与处理结果

如果某子策略 30 天信号 < 3，标记"信号荒漠"，考虑放宽参数（仅在样本外验证后）。

---

## Part 8: 待决策的 10 个问题

落代码前需要回答：

### 来自原 Part 5 的 5 个问题

1. **方向**：先深做 1-2 个方案到极致，还是 5 个都做基础版本对比？
2. **周期**：以 1h 为主可以吗？还是想试 4h 或 15m？
3. **多空**：只做多 / 只做空 / 多空都做？
4. **目标用途**：纯回测研究 / 接入现有 paper trading daemon 实盘？
5. **样本外严格度**：愿意为 2026-04+ 留出多大窗口不动？

### 来自原 Part 11 的 5 个问题

6. **是否启用 H（Taker 过滤）**？前提：确认 taker_buy_base_volume 是币安现货还是合约
7. **多策略资金管理**：独立账户 / 优先级队列？
8. **Swing 延迟**：用 provisional swing（实时更新）还是接受滞后？
9. **异常 K 线**：是否在数据预处理阶段就标记并隔离？
10. **集成路径**：先独立研究 / 直接接入 strategy_evaluator？

---

## Part 9: 推荐实施顺序

1. **数据完整性检查 + Part 6 的特征预计算**
2. **方案 A 单独跑通基线**（最简、最稳）
3. **方案 B、C 分别叠加 A 的过滤器**
4. **组合 I（A+B+C）完整回测**
5. **方案 D 独立实现 + 与组合 I 相关性分析**
6. **F、G 进入 P1 测试，H 作为过滤器全局启用**

**P0 三件套（最小可用）**：A + B + H
**P1 扩展**：C + D + G（相关性 < 0.4 才纳入）
**P2 备选**：F

---

## 文档说明

- **本精简版**：800 行，9 个 Part，落地必需内容
- **完整推敲版**：`5.纯K线策略方案_Opus4.7_full.md`（2782 行，36 Part），包含 7 轮推敲全过程，备查
- **复盘结论**：第 3 轮（原 Part 16）之后边际收益快速下降，第 5 轮之后是"为推敲而推敲"。最终落地价值集中在原 Part 0/1/2/7/9/12/16/22，已并入本精简版

下一步：用户回答 Part 8 的 10 个决策点 → 开始落代码（从 P0 三件套 A+B+H 开始）。
