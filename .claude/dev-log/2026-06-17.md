# Dev Log — 2026-06-17

本会话主题：**Phase 6 可编码门禁项收口** + **前端 dashboard 接真实后端**
+ **60 天守护进程** + **实盘启动门禁入口** + **上交易所前置线（testnet bug/权限校验/覆盖率）**
+ **testnet 集成冒烟** + **testnet 实测打通（ExchangeBroker 查单/撤单修复）**
+ **Phase 7 规划 + Stage 0（市价单 spike）+ Stage 1（执行适配层）+ Stage 2（broker 无关 runner）
+ Stage 3（daemon --broker exchange）+ Stage 4（订单护栏 + 健康巡检扩展）**
+ **60 天 paper 实跑准备（修 live 数据/冷启动两个 bug + 启动 smoke）**
+ **Phase 7 真机 testnet 验证（Stage 3-4 五项核对全 PASS，零代码改动）**。
测试基线 146 → **316 passed**，覆盖率 86% → **89%**（门禁 §4 >80%）。

提交时间线（均已推送 master）：
`b4230fc` → `34267ac` → `c226975` → `5a8c1f7` → `bd22d46` → `f3aa1bc` → `a557712` → `a45d305` → `1886d8d` → `46abe31`(本日志归档) → `def1052`(守护进程) → `7f0ed70`(门禁入口) → `971e7a2`(健康巡检) → `54cb426`(60天启动bat) → `b1fa455`(上交易所前置线) → `0f7e05e`(testnet冒烟) → `7b506a3`(testnet实测打通) → `2d22db9`(Phase7 Stage0) → `37eec03`(Phase7 Stage1) → `3cdca6a`(Stage2 broker无关runner) → `283fc80`(Stage3a 交易所适配器) → `e7f2e75`(Stage3b daemon exchange) → `4e5ac52`(Stage4a 订单护栏) → `1ef1132`(Stage4b 健康巡检扩展) → `5c01be0`(plan文档标完成) → `3f587c5`(dev-log Stage0-4) → `1ce60eb`(纳入phase6 plan) → `2d71f70`(fix:live公共行情keyless) → `45a4321`(fix:冷启动种子仅预热)

---

## Task A — ExchangeBroker 适配层（Phase 6 代码缺口）

三层 Broker（Paper→Exchange→Live）补上中间层。`ExchangeBroker(BrokerInterface)`
用 ccxt 适配交易所，testnet/查询为主、不下真单；构造器加 `exchange=` 依赖注入口
便于离线测试。Live Broker（真实下单）仍属 Phase 7+。

**Files:** `src/execution/exchange_broker.py`（新）、`src/execution/__init__.py`、
`tests/unit/test_exchange_broker.py`（13 离线用例，FakeExchange 注入不触网）
**验证:** 13 passed；基线 146→159。**Status:** ✅（提交 `b4230fc`）

## Task B — 门禁文档（§5 操作/故障手册）

补齐 LIVE_TRADING_CHECKLIST §5 两项未勾选项，命令/路径按仓库实际核实
（含 `-p no:asyncio` 须手动加、`monitor_metrics` 手动补建、Grafana 数据源已修坑）。

**Files:** `docs/standards/OPERATIONS_MANUAL.md`、`TROUBLESHOOTING.md`（新）、
`LIVE_TRADING_CHECKLIST.md`（§5 勾选）
**Status:** ✅（提交 `34267ac`）

## Task C — 前端接真实后端（支线）

v0 生成的 `frontend/`（Next.js）由全 mock 改接真实后端并纳入 git。
- 后端新建 FastAPI `src/api/`（app/service）：首次请求跑一次 Paper Trading 并
  进程内缓存，映射 `runner.lots`/`trade_history`/`snapshots` → 前端契约；9 端点 + CORS。
- 前端 `lib/api.ts` 由 mock 改 `fetch(${NEXT_PUBLIC_API_BASE})`，types 不变。
- 补 `react-is`（recharts peer dep，原 lock 缺失致图表 500）。
- 一键 `start_dashboard.bat`：装依赖→后端(8000)+前端(**3001**，避开 Grafana 占的
  3000)→弹浏览器；**统一 npm**（删 pnpm-lock，避免 pnpm 路径重现 500）。

**实跑修的真 bug：** ① 3000 端口被 Grafana 占→前端改 3001 + CORS 加 3001；
② bat 里 corepack 无 `call` 致 bat 提前退出→去掉 corepack；③ recharts 缺 react-is。

**Files:** `src/api/{__init__,app,service}.py`、`frontend/**`、`tests/unit/test_api.py`(7)、
`requirements.txt`（放开 fastapi/uvicorn）、`start_dashboard.bat`、`.gitignore`
**验证:** 6 页面路由 200、CORS 通、数据对账；基线 166。**Status:** ✅（提交 `c226975`）

## Task D — 前端数据保真三连

- **实时行情**：`src/api/market.py` ccxt 拉 Binance 6 交易对 24h ticker（一次
  `fetch_tickers`，15s TTL，20s 超时），`service.tickers()` 优先实时、失败回退本地
  派生（不 500）。修了回退分支缺 `logger` import 的 bug。（提交 `5a8c1f7`）
- **订单时间戳**：`PaperBroker.place_order` 加可选 `timestamp`（省略仍 now()），
  runner 传当根 bar 时间 → 订单/createdAt 用真实成交时刻，时间轴可复盘。（`bd22d46`）
- **winRate 精确化**：runner 收集逐笔平仓 `closed_trades:{tag,time,profit}`，
  service 用其算精确胜率（删 FIFO 近似）；对账 56 笔盈亏和==realized_pnl，
  58.9%→真实 96.4%。（`f3aa1bc`）

**Files:** `src/api/{market,service}.py`、`src/execution/{paper_broker,paper_trading_runner}.py`、
`tests/unit/test_api.py`
**Status:** ✅

## Task E — 测试覆盖率达标（门禁 §4）

补齐此前 0% 的纯逻辑模块，覆盖率 **69%→86%**：simple_ma、buy_and_hold、
backtest.report_generator、data.report_generator、bias_detector、param_scanner、main。
剩余低覆盖为网络/DB 依赖（exchange 17%/database 46%/market 46%）。

**Files:** `tests/unit/test_{strategies_simple,backtest_report_generator,
data_report_generator,bias_detector,param_scanner,main}.py`、`LIVE_TRADING_CHECKLIST.md`(§4 勾选)
**验证:** 195 passed，coverage 86%。**Status:** ✅（提交 `a557712`）

## Task F — §2 风控测试自动化

`scripts/verify_risk_controls.py`：一次跑完 §2 全 6 场景（日亏损3%/连亏5笔/
数据异常/API失败/人工恢复/紧急停止），PASS/FAIL + 退出码（6/6，exit 0）；
`tests/unit/test_verify_risk_controls.py` 纳入 CI。checklist §2 六项勾选。

**验证:** 脚本 6/6 PASS exit 0；基线 202。**Status:** ✅（提交 `a45d305`）

## Task G — preflight 总门禁自检

`scripts/preflight_check.py`：聚合所有可机器验证门禁项——Python/依赖/实盘安全
开关/风控参数/门禁文档(§5)/Broker三层/§2熔断/测试套件+覆盖率(§4)，出总表 +
退出码（8/8 PASS，exit 0）。**显式单列 MANUAL 门禁**（§1 60天运行、§3 3周稳定、
API权限、资金≤$500、签风险书、双重确认）并强调“自检通过≠可实盘”，避免假绿灯。
fast 检查纳入 CI（不含子进程避免递归）。修了末尾 ⚠️ emoji 在 GBK 控制台崩溃的问题。

**Files:** `scripts/preflight_check.py`、`tests/unit/test_preflight_check.py`、
`LIVE_TRADING_CHECKLIST.md`（启动章节加 preflight 引用）
**验证:** 8/8 PASS exit 0；基线 210。**Status:** ✅（提交 `1886d8d`）

## 守护进程（日志归档后补提，提交 `def1052`）

`scripts/run_paper_trading_daemon.py`：单次批量 → 可连续运行 N 天（默认 60）的
模拟盘。每根 bar 取数→跑策略→PaperBroker 成交→风控→落库→跨日出日报；双模
实时（Binance OHLCV）/`--replay` 回放。**原子写检查点支持崩溃重启续跑，且续跑
== 连续运行逐位一致**（已测）。配套把 `PaperTradingRunner` 抽出 `process_bar`，
run() 与守护进程共用、批量行为不变。§1 的 60 天实跑本身仍是时间轨道。
**Files:** `scripts/run_paper_trading_daemon.py`、`tests/unit/test_paper_trading_daemon.py`、
`src/execution/paper_trading_runner.py`。**Status:** ✅

## Task H — 实盘启动门禁入口（双重确认开关，Phase 6 最后一个可编码项）

把 checklist 里只有伪代码的"双重确认开关"做成真实代码。
`scripts/start_live_trading.py`：`verify_live_trading_checklist()` 纯函数复用
preflight 可自动验证项，但**安全开关极性相反**（preflight 要求
`LIVE_TRADING_ENABLED=False` 作安全默认；实盘启动要求 `=True`，故换成
`check_live_switch_enabled`），任一不过 `raise LiveTradingBlocked`；
`start_live_trading(confirm_fn=input)` 两次人工 YES 确认（input 可注入便于测试）。
**关键诚实点**：门禁 + 双确认全过后，因 Live Broker（真实下单）属 Phase 7+ 未实现
→ 硬 `raise LiveTradingNotReady` 拒绝启动——门禁能拦，交易动作尚不存在不假装跑。
退出码 1=拦截/取消、2=全过但未就绪。

**Files:** `scripts/start_live_trading.py`（新）、`tests/unit/test_start_live_trading.py`
（6 契约：极性 + 三条确认路径）、`LIVE_TRADING_CHECKLIST.md`（双确认章节指向真实入口）
**验证:** 6 passed；基线 214→**220**。CLI 实测默认环境拒启动 exit 1。
**Status:** ✅（提交 `7f0ed70`，已推送）

## Task I — 守护进程 shakedown 验证 + 运行健康巡检

**① shakedown（无新代码，端到端验证守护进程工具本身）**：用独立 scratch 目录
跑 `--replay generate --no-db`，确认产出每日 JSON+MD 日报、检查点字段正确；
做了**续跑一致性对账**——一次性跑满 5 天 vs 分段（2 天→中断→续跑到 5 天），
检查点 broker/runner/risk/strategy **逐位一致**，日报内容一致，唯一差异是
`generated_at` 墙钟时间戳（benign）。结论：§1 崩溃续跑机制端到端可信。
（观察：示例数据下网格 5 天只建仓未平仓 `realized_pnl=0`，属数据/区间非 bug。）

**② 健康巡检脚本**：`scripts/check_daemon_health.py`，60 天值守期旁路核验。
`assess_health()` 纯函数（无 I/O，可单测）核验：检查点存在/可解析、风控状态
（STOPPED→FAIL 需人工 reset / PAUSED→WARN 等 resume / ACTIVE→PASS）、检查点
新鲜度（mtime 超阈值默认 12h→WARN 疑似进程停）、`.resume` 标志→WARN、
**日报数量对账（份数==day_count）+ 连续无缺口（日期不得跳缺）→FAIL**。
退出码 0=无 FAIL / 1=有 FAIL。

**Files:** `scripts/check_daemon_health.py`（新）、`tests/unit/test_check_daemon_health.py`
（9 用例：健康/缺状态/STOPPED/PAUSED/陈旧/resume/日期缺口/数量不符/_date_gaps）
**验证:** 9 passed；基线 220→**229**。CLI 冒烟：真实 4 天 replay→全 PASS exit 0；
注入 STOPPED→exit 1。**Status:** ✅

## 守护进程 60 天一键启动（提交 `54cb426`）

`start_paper_60d.bat`：双击启动 `--days 60 --no-db`，PowerShell `Tee-Object`
终端实时显示 + 落盘 `logs/paper_60d_<时间戳>.log`，头部内置值守提示（保活/
不带 --fresh 续跑/巡检命令/`.resume` 恢复）。`.gitignore` 补忽略运行时产物
（`logs/`、检查点、日报目录）。**Status:** ✅
（另：本会话已实跑 §1 shakedown，replay 5 天验证环境通；60 天实时运行由用户在
持久终端发起——会话级进程跑不了两个月。实时行情连通性预检通过：拉到真实 4h K线。）

## Task J — 上交易所前置线（testnet bug 修复 + API Key 权限校验 + 覆盖率）

为接 Binance Demo/testnet 铺路，按 1→2→3 推。
**① fix ExchangeBroker testnet 真 bug（重要安全）**：`src/execution/exchange_broker.py`
原先内部构造 ccxt 只设 `options.testnet=true`——**不切 endpoint，请求实际打主网**
（用主网 key 即真实下单）。改用 ccxt 标准 `set_sandbox_mode(testnet)`，删无效选项。
实证：testnet=True 修后 endpoint 切到 `testnet.binance.vision`（原 `api.binance.com`）。
注入分支（FakeExchange）不动。+2 回归测试。
**② feat verify_api_key_permissions**：`scripts/verify_api_key_permissions.py`，连
交易所查 `apiRestrictions`（ccxt `sapiGetAccountApiRestrictions`）。纯函数
`assess_api_key_permissions()` 断言：禁提币/禁合约=FAIL、允许现货+读取=必需、
无 IP 白名单/单独开杠杆=WARN。退出码 0/1/2，复用 ExchangeBroker（连带吃 sandbox
修复）。checklist「API Key 验证步骤」伪代码指向真实入口。+9 测试。
**③ 覆盖率**：exchange_broker→100%、`src/api/market.py` 46%→100%、
`src/data/exchange.py`（记忆点名的 17%）→94%。

**Files:** `src/execution/exchange_broker.py`、`scripts/verify_api_key_permissions.py`（新）、
`tests/unit/{test_exchange_broker(+2),test_verify_api_key_permissions,test_market,test_data_exchange}.py`、
`LIVE_TRADING_CHECKLIST.md`（伪代码指针）
**验证:** 基线 229→**254**，覆盖率 86%→**89%**。**Status:** ✅（提交 `b1fa455`，已推送）

## Task K — Binance testnet 集成冒烟

用户提"把 daemon 接 ExchangeBroker testnet"。**先核实后否决直接切**：读 runner 发现
`PaperTradingRunner` 深度耦合 PaperBroker（`place_order(...,timestamp=)`、判 `status=="filled"`、
用 `broker.commission/slippage/initial_balance/get_statistics`、检查点序列化 `broker.balance/
positions/orders`），ExchangeBroker 全不匹配（向交易所下真单/异步成交/状态在交易所端）。
直接切 = Phase 7 重写执行模型 + 打破"回测逐位一致"。经询问，改做右尺寸方案：

`scripts/testnet_smoke.py`——接 Demo 前端到端连通验证，**不动主 daemon**。四步：
①安全护栏（必须 BINANCE_TESTNET=true+有 key，否则 exit 2 绝不碰主网）；②权限校验
（复用 verify_api_key_permissions，testnet 无 SAPI 则 WARN 跳过）；③查余额（核心连通证明）；
④下单生命周期（半价限价买单挂着不成交→查回→撤单，零成交风险）。
**暴露 Phase 7 待修缺口**：`ExchangeBroker.cancel_order(order_id)` 未传 symbol，ccxt
binance 撤单必失败；脚本记 WARN 并用底层 `exchange.cancel_order(id,symbol)` 兜底清理挂单。

**Files:** `scripts/testnet_smoke.py`（新）、`tests/unit/test_testnet_smoke.py`
（6：safe_limit_order_params 纯函数 + 两条护栏 exit 2；集成步骤需凭据无法离线测）
**验证:** 基线 254→**260**；无 key 实跑护栏 exit 2 正确。**Status:** ✅（提交 `0f7e05e`，已推送）

## Task K-2 — testnet 实测打通（ExchangeBroker 查单/撤单修复）

用户拿到 testnet key（GitHub 登录 testnet.binance.vision 生成 HMAC key）填入 `.env`
（从 .env.example 创建，gitignore 忽略，未入库）。实跑冒烟连揪 3 个问题并修复：
- **get_order_status 漏传 symbol**（新发现）→ 币安查单返回 None。
- **cancel_order 漏传 symbol**（原标 Phase 7 待修，冒烟一跑即暴露）→ 撤单失败。
  修法：ExchangeBroker 下单记 `order_id→symbol`，查单/撤单回查传入，不改接口签名。
- **安全限价单 factor 0.5 被 PERCENT_PRICE_BY_SIDE 拒**（价格带 floor=0.5 卡边界）
  → 改 0.7。
另：`verify_api_key_permissions.py` 在 testnet exit 2 属正常——testnet 无 SAPI
端点查不了 apiRestrictions，权限校验只能主网用。

**testnet 端到端实测全 PASS**：查余额(USDT 10000/BTC 1.0) / 下单(id 真实) /
查回 / 撤单。**Files:** `src/execution/exchange_broker.py`、`tests/unit/test_exchange_broker.py`
（FakeExchange 加 symbol + 回归，16 passed）、`scripts/testnet_smoke.py`。
**验证:** 基线 260→**261**。**Status:** ✅（提交 `7b506a3`，已推送）

## Task L — Phase 7 规划 + Stage 0（守护进程接 testnet）

用户要"把守护进程接 testnet 规划一下"。先讲清这是 Phase 7 重写非接线（runner 深耦合
PaperBroker 即时成交+本地记账，ExchangeBroker 异步成交+状态在交易所端）。出规划
`.claude/plans/phase7-daemon-testnet.md`（**未入库,plans 目录一直未跟踪**）。
**已定决策**：v1 市价单起步（限价网格留 v2）、完整重构（runner 解耦窄 broker 协议、
账务内移，非 shim）、明确放弃 testnet 续跑逐位一致。五 Stage：0 spike→1 执行适配层→
2 broker 无关 runner(最高风险)→3 daemon --broker exchange→4 安全运维。

**Stage 0 完成**：`testnet_smoke.py` 加 `--market-spike`（极小市价买+卖回）+ 纯函数
`extract_fill`（优先 place 结果、回退查单）。**实测关键结论：币安 testnet 市价单在
place_order 返回里直接带成交价量（来源=place）→ Stage 1 place_and_confirm 对市价单
无需轮询循环**。顺带修过时注释。
**Files:** `scripts/testnet_smoke.py`、`tests/unit/test_testnet_smoke.py`（+4 extract_fill）、
`.claude/plans/phase7-daemon-testnet.md`（新,本地）
**验证:** 基线 261→**265**；testnet 实跑 spike 全 PASS。**Status:** ✅（提交 `2d22db9`，已推送）

## Task M — Phase 7 Stage 1（执行适配层 ExchangeExecutor）

`src/execution/exchange_execution.py`：
- `size_order`：精度取整 + 最小下单量/minNotional 守卫（读 ccxt market limits）。
  **真机硬化**：`amount_to_precision` 对低于精度的极小量抛 `ccxt.InvalidOrder`，
  try/except 归一成 rejected 不冒泡——单测 FakeExchange 不暴露，真机才踩到。
- `place_and_confirm`：size→下单→确认，归一化 `filled`/`partial`/`timeout`/`rejected`+
  真实价量。市价单同步取价（Stage0 结论），轮询/超时为 v2 限价单预留（注入时钟）。
- `extract_fill` 从 scripts 移到 src（修正依赖方向，testnet_smoke 改为复用）。

**Files:** `src/execution/exchange_execution.py`（新）、`tests/unit/test_exchange_execution.py`
（13：sizing 4 + place_and_confirm 5 + extract_fill 3 + 精度异常回归）、`scripts/testnet_smoke.py`
（import 改 src）、`.claude/plans/phase7-daemon-testnet.md`（Stage 1 标完成,本地）
**验证:** 基线 265→**278**；真机复验 testnet 市价买/卖回 filled、极小量 rejected。
**Status:** ✅（提交 `37eec03`，已推送）

---

## Task N — Phase 7 Stage 2（broker 无关 runner，最高风险）

runner 经济参数收口，主循环不再读 broker 内部。**方案=只搬 config**（用户在两案中拍板，
非完整 ledger 内移）：新增 `ExecutionConfig`（frozen：commission/slippage/initial_balance）
+ `from_broker()` 快照 + `RunnerBroker`（`typing.Protocol`，**刻意不继承 BrokerInterface
故 ExchangeBroker 不受影响**）。sizing/profit 改读 `self.exec_cfg`；加可选 `exec_config=`
注入口（默认 from_broker → 所有现有调用点不变）。get_statistics/get_trade_history 暂仍走
broker 协议（完整账本内移留待将来）。

**Files:** `src/execution/paper_trading_runner.py`、`tests/unit/test_paper_trading_runner.py`（+2）
**验证:** paper 路径 **bit-for-bit 不变（278）**，+2 测试→**280**。daemon 检查点仍直接序列化
PaperBroker.balance/positions/orders（paper 专属，Stage 3 才分支）。
**Status:** ✅（提交 `3cdca6a`，已推送）

---

## Task O — Phase 7 Stage 3（daemon --broker exchange，testnet 真实下单）

**3a 适配器** `src/execution/exchange_runner_broker.py`：`ExchangeRunnerBroker` 把
`ExchangeExecutor` 适配成 RunnerBroker 协议。place_order 经 place_and_confirm：filled/partial
→记本地账本并**归一为 filled**（携真实 filled_amount，partial 剩余不重试靠对账兜底）、
timeout→记 `_unconfirmed`、rejected→原样返回。get_balance/get_position 透传真实交易所；
statistics/history 走本地账本。构造时快照 `initial_balance/initial_position`（**testnet 账户
有底仓如 BTC 1.0，对账必须按 delta**）。纯函数 `assess_position_drift`。

**3b daemon**：`--broker {paper,exchange}`（默认 paper）+ `--drift-abs/--drift-rel`；`_build`
分流 + `_make_exchange_broker()` 注入缝；**硬护栏强制 `BINANCE_TESTNET=true`+配 key 否则
拒启（绝不碰主网）**；每 bar `_reconcile_drift`→超阈值 `risk.emergency_stop`；检查点按模式
分支（exchange 存 `adapter.state_dict()`，**paper 段逐字不变**，`test_resume_equals_continuous`
仍绿证明）；重启对 `_unconfirmed` 查交易所，仍挂单→`SystemExit` 拒绝静默续跑。

**Files:** `src/execution/exchange_runner_broker.py`（新）、`scripts/run_paper_trading_daemon.py`、
`src/execution/__init__.py`、`tests/unit/test_exchange_runner_broker.py`（+11）、
`tests/unit/test_daemon_exchange.py`（新 +8）
**验证:** paper 零回归，基线 280→**299**。**真机 testnet 端到端=人工**（本会话无网未跑）。
**Status:** ✅ offline 部分（提交 `283fc80` 3a + `e7f2e75` 3b，已推送）

---

## Task P — Phase 7 Stage 4（订单级护栏 + 健康巡检扩展）

**4a 护栏** `src/execution/order_guard.py`：`OrderRateGuard` 单笔名义额上限/最小间隔/日订单数
（RISK_CONTROLS：0.20/300s/10）。**间隔按 bar ts（下单决策周期）判**——同一 bar 多笔 grid lot
视作一次决策全放行（用户拍板）；单笔上限+日订单数逐单。接进 ExchangeRunnerBroker（可选
`guard=`）+ `_errors` 计数，state_dict 增 errors。daemon 加 `--max-position-per-trade/
--min-trade-interval/--max-trades-per-day`。

**4b 巡检** `check_daemon_health.py` `assess_health` 扩 exchange 核验（靠 broker 含
`unconfirmed` 键区分，paper 形态跳过、向后兼容）：卡单（unconfirmed 非空→WARN）、下单错误率
（errors/(errors+fills) 超 `--max-order-error-rate` 默认 0.2→WARN）。持仓漂移已由 daemon 实时
熔断→风控状态 FAIL 覆盖，离线不重复判。

**Files:** `src/execution/order_guard.py`（新）、`src/execution/exchange_runner_broker.py`、
`src/execution/__init__.py`、`scripts/run_paper_trading_daemon.py`、`scripts/check_daemon_health.py`、
`tests/unit/test_order_guard.py`（新 +7）、`tests/unit/test_exchange_runner_broker.py`（+2）、
`tests/unit/test_check_daemon_health.py`（+5）
**验证:** paper 零回归，基线 299→**313**。
**Status:** ✅ offline 部分（提交 `4e5ac52` 4a + `1ef1132` 4b，已推送）

**Phase 7 testnet 线（Stage 0-4）代码全部完成。唯一剩余=真机 testnet 验证（人工）**：
`python scripts/run_paper_trading_daemon.py --broker exchange --timeframe 1m --days 1
--min-trade-interval 30 --no-db`，核对 ① 订单在 testnet 端存在 ② 余额/持仓按 delta 对得上账本
③ 超单笔上限/日订单数被拒并计 errors ④ 制造漂移触发熔断 ⑤ `check_daemon_health.py`
卡单/高错误率 WARN。限价网格执行=v2；真实主网/Live Broker=Phase 7+ 后续。

---

## Task Q — 60 天 paper 实跑准备（两个 daemon fix + live 启动 smoke）

用户让"起 60 天 paper 跑"。起之前先核验，**暴露并修了两个真 bug**：

1. **`fix 2d71f70`：live 公共行情误用 testnet key 打主网**。`_fetch_live_df` 原用
   `create_binance_client(testnet=False)`，把 `.env` 的 testnet key 传给主网公开行情→
   Binance 拒 `-2008 Invalid Api-Key`→连续 API 失败被风控暂停。公开 OHLCV 无需签名：
   `create_binance_client` 加 `public=True` 强制无凭据，daemon live fetch 改用之。
   实测取回 199 根真实 4h bar。+1 回归测试。
2. **`fix 45a4321`：live 冷启动种子被回填成"运行日"**。原 `_run_live` 冷启动用 200 根
   种子（~28 天 4h 历史）经 `_consume_new_bars` 全量回填→day_count 瞬间≈28、出 28 份日报，
   连 `--fresh` 也非真 day-0，与"60 天真实连续运行"门禁冲突（旧 `data/paper_daemon_state.json`
   的 day_count=28 就是这么来的）。**用户拍板改 daemon**：抽 `_seed_live_warmup`——种子只定
   网格区间 + 历史上下文，把 last_bar_ts 推进到种子末根，day_count 只随启动后真实新 bar 推进；
   **replay 模式保留回填语义**（加速跑历史的本意）。+2 测试（冷启动零回填 / 新 bar 被处理）。

**live 启动 smoke（scratch 目录，1m 加速看真 tick，不触 data/ 生产）**：跑 ~80s——
baseline 末根 05:28:00 → 运行后 last_bar_ts **05:29:00（真处理了一根启动后的新 live bar）**；
`day_count=0`（**零回填**，冷启动 fix 在真实循环里成立）；`risk=ACTIVE / api_failures=0`
（**keyless fetch 通、无 -2008、不暂停**，数据 fix 成立）；无日报（80s 内未跨日，符合预期）。
scratch 已清，data/ 未动。

**收尾**：把旧 28 天回填产物（state + 56 份报告）归档到 `data/_archive_backfill_20260617/`
（可逆 mv，data/ 已 gitignore），state 目录清空→`start_paper_60d.bat` 冷启动 day-0。

**Files:** `src/data/exchange.py`（+public 参数）、`scripts/run_paper_trading_daemon.py`
（live fetch keyless + `_seed_live_warmup`）、`tests/unit/test_data_exchange.py`（+1）、
`tests/unit/test_paper_trading_daemon.py`（+2）
**验证:** 基线 313→**316 passed**；live smoke 端到端确认真实 tick + 零回填 + 无 -2008。
**Status:** ✅ 代码就绪（提交 `2d71f70` + `45a4321`，已推送）。**60 天实跑=时间轨道，用户在本机
用 `start_paper_60d.bat` 持久跑**（Claude 会话无法托管，会随会话结束被杀）。

---

## Task R — Phase 7 真机 testnet 验证（Stage 3-4 五项核对全部 PASS）

执行 Task P 末尾留下的唯一待办——Phase 7 testnet 线（Stage 0-4）的**真机验证**。
零代码改动，纯端到端验证；全部在 `data/_phase7_scratch/` 隔离目录（验证后已清），
已跟踪文件零改动，testnet 沙盒账户因 ① 真买入了 0.06006 BTC（花 ~3939 USDT，测试币）。

前置核验：`.env` testnet key/secret 就绪（TESTNET=true, LIVE_TRADING_ENABLED=false
安全默认）；`pytest -q` 基线 **316 passed** 与 dev-log 记录一致；`testnet_smoke.py`
连通正常（余额 USDT~10000/BTC 1.0，下单/查回/撤单全 PASS）。

- **① 执行链路 + delta 对账（PASS）**：`daemon --broker exchange --replay generate
  --days 1`（scratch state-file）跑出 3 笔真实 testnet 市价买单，order_id
  5821092/94/95，成交价 65590.01（≈实时市价 65851），每笔 0.02002 BTC。对账：
  `fetch_order` 确认 3 单 testnet 端 status=closed、filled/avg_px 与 ledger 逐笔一致；
  BTC 持仓 delta **0.06006 == local_net（DRIFT=0.0 完美对账）**；USDT delta 3939.34
  ≈ spent+comm。每 bar `_reconcile_drift` 无熔断，风控 ACTIVE。
- **③ 护栏拒单 + errors 计数（PASS）**：`--max-position-per-trade 0.00001
  --max-trades-per-day 2`（名义额上限 ~0.06 美元）。全部下单被护栏拒（"单笔名义额
  605.46 > 上限 0.06"），`errors=3` 正确计数、`ledger=0`（被拒不记账）；**testnet 端
  余额/持仓 0 漂移**（被拒的单确实没下到交易所），风控仍 ACTIVE。
- **④ 持仓漂移熔断（PASS）**：复用 daemon 组件做真机集成——连真实 testnet 查持仓，
  快照 initial_position=1.06006，设 local_net=0.0001 制造偏离（real_delta=0 ≠
  local_net），调 `_reconcile_drift` → `emergency_stop` → 风控 **ACTIVE→STOPPED**。
  零新增下单，纯对账路径触发。
- **⑤ 健康巡检 exchange 核验（PASS）**：对真实跑出的检查点跑 `check_daemon_health.py`：
  ③的 state_guard（errors=3/fills=0）→ **下单错误率 100% > 20% 触发 WARN**；注入
  unconfirmed 的 state_stuck → **卡单 "2 笔待确认订单" 触发 WARN**；其余 6 项 PASS。

**结论：Phase 7 testnet 线（Stage 3-4）真机端到端全部验证通过。** 执行链路真实下单 +
逐笔对账零误差 + 护栏真实拦截 + 漂移真实熔断 + 健康巡检真实 WARN，代码已就绪。
唯一未做：限价网格执行（v2，留待将来）；真实主网/Live Broker（Phase 7+ 后续）。

---

## Phase 6 状态

代码侧门禁项**已全部清完**并有统一自检入口 `preflight_check.py`：
- ✅ §2 风控测试（自动化脚本 + CI）
- ✅ §4 单测覆盖率 >80%（86%）
- ✅ §5 操作/故障手册
- ✅ ExchangeBroker 适配层（Phase 5-6）
- ✅ §1 60 天连续运行守护进程（工具就绪 + shakedown 端到端验证续跑一致，实跑仍是时间轨道）
- ✅ 实盘启动门禁入口（双重确认脚手架，人工 YES/YES 仍手动）
- ✅ 守护进程运行健康巡检（60 天值守期旁路核验工具）

## 后续待办

- **Phase 6 剩余全是时间/人工轨道**，无代码可写：§1 paper trading 连续运行 60 天
  （每日摘要齐全，守护进程已就绪只等真实跑满）、§3 连续 3 周无故障、API Key 权限
  受限、初始资金 ≤$500、签风险确认书、实盘双确认的人工 YES/YES（脚手架已成代码）。
- 前端仍存缺口（低优先）：启停 PATCH 为 no-op（无常驻策略进程）；price-action
  策略后端不存在（Phase 7+ 研究线，不进主线）。
- **Phase 7 testnet 线真机验证已完成（Task R，五项核对全 PASS）**；限价网格执行=v2、
  真实主网/Live Broker=Phase 7+ 后续，均无近期代码。
- 真正的下一步是**启动 60 天 paper 连续运行**并积累门禁所需运行记录。
