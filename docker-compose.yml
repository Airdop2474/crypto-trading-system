# 安全建议：生产环境使用 Docker secrets 或 *_FILE 变量替代明文密码
# - GF_SECURITY_ADMIN_PASSWORD__FILE=/run/secrets/grafana_password
# - TIMESCALE_PASSWORD_FILE=/run/secrets/timescale_password

# Docker 日志轮转全局默认（各服务可覆盖）
x-logging: &default-logging
  driver: json-file
  options:
    max-size: "10m"
    max-file: "3"

# 安全基线（所有服务继承）
x-security: &default-security
  cap_drop:
    - ALL
  security_opt:
    - no-new-privileges:true

services:
  timescaledb:
    image: timescale/timescaledb:2.17.0-pg16
    container_name: crypto_trading_db
    logging: *default-logging
    <<: *default-security
    cap_add:
      - CHOWN
      - SETUID
      - SETGID
      - DAC_OVERRIDE
    environment:
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:?POSTGRES_PASSWORD must be set in .env}
      POSTGRES_DB: ${POSTGRES_DB:-crypto_trading}
      POSTGRES_USER: ${POSTGRES_USER:-postgres}
    volumes:
      - timescaledb_data:/var/lib/postgresql/data
      - ./config/sql:/docker-entrypoint-initdb.d
    # 生产环境：移除端口映射，仅通过内部网络访问
    # 开发环境：保留端口映射便于本地调试
    ports:
      - "${POSTGRES_PORT:-5432}:5432"
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 10s
      timeout: 5s
      retries: 5

  redis:
    image: redis:7.4-alpine
    container_name: crypto_trading_redis
    logging: *default-logging
    <<: *default-security
    cap_add:
      - CHOWN
      - SETGID
      - SETUID
    environment:
      - REDIS_PASSWORD=${REDIS_PASSWORD:?REDIS_PASSWORD must be set in .env}
    command: redis-server --appendonly yes --requirepass ${REDIS_PASSWORD}
    volumes:
      - redis_data:/data
    # 生产环境：移除端口映射
    ports:
      - "${REDIS_PORT:-6379}:6379"
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "redis-cli -a \"$$REDIS_PASSWORD\" ping | grep -q PONG"]
      interval: 10s
      timeout: 3s
      retries: 5

  grafana:
    image: grafana/grafana-oss:10.4.12
    container_name: crypto_trading_grafana
    logging: *default-logging
    <<: *default-security
    environment:
      - GF_SECURITY_ADMIN_USER=${GRAFANA_ADMIN_USER:-admin}
      - GF_SECURITY_ADMIN_PASSWORD=${GRAFANA_ADMIN_PASSWORD:?GRAFANA_ADMIN_PASSWORD must be set in .env}
      - POSTGRES_USER=${POSTGRES_USER:-postgres}
      - POSTGRES_DB=${POSTGRES_DB:-crypto_trading}
      - POSTGRES_PASSWORD=${POSTGRES_PASSWORD:?POSTGRES_PASSWORD must be set in .env}
    volumes:
      - grafana_data:/var/lib/grafana
      - ./config/grafana:/etc/grafana/provisioning
    ports:
      - "3000:3000"
    depends_on:
      timescaledb:
        condition: service_healthy
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "wget", "--spider", "--timeout=3", "http://localhost:3000/api/health"]
      interval: 30s
      timeout: 10s
      retries: 3

  # 交易系统主服务
  trading_system:
    build: .
    container_name: crypto_trading_app
    logging: *default-logging
    <<: *default-security
    user: "1000:1000"
    depends_on:
      timescaledb:
        condition: service_healthy
      redis:
        condition: service_healthy
    env_file:
      - .env
    environment:
      DATABASE_URL: postgresql://${POSTGRES_USER:-postgres}:${POSTGRES_PASSWORD}@timescaledb:${POSTGRES_PORT:-5432}/crypto_trading
      REDIS_URL: redis://:${REDIS_PASSWORD}@redis:${REDIS_PORT:-6379}/0
      TIMESCALE_HOST: timescaledb
    volumes:
      - ./logs:/app/logs
      - ./data:/app/data
      - ./config:/app/config
    ports:
      - "8000:8000"
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 10s
    command: python -m uvicorn src.api.app:app --host 0.0.0.0 --port 8000

  # Paper Trading 守护进程（自动重启）
  paper_daemon:
    build: .
    container_name: crypto_trading_paper_daemon
    logging: *default-logging
    <<: *default-security
    user: "1000:1000"
    depends_on:
      timescaledb:
        condition: service_healthy
      redis:
        condition: service_healthy
    env_file:
      - .env
    environment:
      DATABASE_URL: postgresql://${POSTGRES_USER:-postgres}:${POSTGRES_PASSWORD}@timescaledb:${POSTGRES_PORT:-5432}/crypto_trading
      REDIS_URL: redis://:${REDIS_PASSWORD}@redis:${REDIS_PORT:-6379}/0
      TIMESCALE_HOST: timescaledb
      # GC 调优：降低阈值让 ccxt 临时对象更快回收，减少内存抖动
      PYTHONMALLOC: malloc
      PYTHONGCSEED: "1"
    volumes:
      - ./logs:/app/logs
      - ./data:/app/data
      - ./config:/app/config
    mem_limit: 512m
    memswap_limit: 768m
    restart: unless-stopped
    command: python -X gc=1 scripts/run_paper_trading_daemon.py --days 20 --timeframe 1h
    # 不设 ports —— 守护进程不需要对外暴露端口
    # 通过 checkpoint 文件（data/paper_daemon_state.json）实现崩溃恢复

volumes:
  timescaledb_data:
  redis_data:
  grafana_data:

networks:
  default:
    name: crypto_trading_network
